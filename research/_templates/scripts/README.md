# Scripts

Place experiment-specific runners and helpers here.

Default expectation:
- keep execution package-local in `run_experiment.py`
- use the shared `scripts/research_export.py` workflow for cell/package export

Examples:
- `run_experiment.py`
- `export_highlights.py`
