# Scripts

This package uses three experiment-local helpers:
- `run_experiment.py` to execute one or more cells from the matrix
- `export_cell_results.py` to export canonical per-cell artifacts
- `export_package_results.py` to export the package-level aggregate
