# Artifacts

- This is a summary-only umbrella package.
- The atomic exported artifacts remain in the constituent FixedDamage packages.
- Use this folder only for future derived summaries if we decide to add them.
