# Recordings (External Only)

Raw match recordings are intentionally excluded from git.

Canonical storage target:
- Hugging Face dataset: `agentdeck/replays`
- Prefix: `2026-q1-benchmark-matrix/`

This folder should only contain pointers/manifests describing where to fetch shards.
