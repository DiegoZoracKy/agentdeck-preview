# Scripts

Execution helpers for this matrix should live here.

Planned:
- `export_highlights.py` - compute viewer tags and curated shortlist

Available:
- `run_matrix_phase.py` - execute matrix cells by selection mode (`preflight`, `phase`, `cells`)
- `turn_budget.py` - compute meaningful turn cap and recommended `max_turns`
- `run_one_match_openai_mini.py` - one real out-of-the-box smoke match (`gpt-4o-mini` vs `gpt-4o-mini`)
