# Recordings (External Only)

Raw match JSON should not be committed to this repository.

Use this folder for pointers/manifests only, for example:
- dataset URI
- snapshot/revision
- shard list

Example pointer:
- `hf://datasets/agentdeck/replays/2026-q1-benchmark-matrix/`
