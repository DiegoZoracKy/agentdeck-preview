# Local Matches

Drop local replay files (`*.json`) in this directory and refresh the viewer.

Then update `manifest.json` so they appear in the in-app "Local Match Library" selector.

You can generate the manifest automatically with:

```bash
node scripts/update_match_manifest.js
```
